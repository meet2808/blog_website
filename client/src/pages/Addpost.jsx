import { PostForm } from "../components";

const Addpost = () => {
    return(
        <>
            <PostForm formtitle={"Write Blog"} isEditable={false} />
        </>
    )
}

export default Addpost;